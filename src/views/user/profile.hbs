<div class="row">
    <div class="col-md-10 col-md-offset-2">
        <h1>Mi Perfil</h1>
        <hr>
        <h2>Mis Ordenes</h2>

        {{# each orders }}
            Nº Orden : {{ this.paymentId }} 
            <div class="">
                <div class="">
                    <ul class="list-group">

                        {{# each this.items }}
                            
                            <li class="list-group-item list-group-item-action list-group-item-light">
                              {{ this.item.nombre }} | {{ this.qty }} Unidad
                                <span class="badge badge-success badge-pill float-right">${{ this.precio }}</span>
                            </li>

                        {{/each}}

                    </ul>
                </div>
                <div class="card-footer">
                    Total Price: <strong>${{ this.cart.totalPrice }}</strong>
                </div>
            </div>

        {{/each}}

    </div>
</div>